# LOG 2026-05-28 10:20 — Fix bind host API (0.0.0.0)

## Contexto

- Domínios da API retornando 502 mesmo com `curl localhost:3333/health` = 200 dentro do container.
- Teste interno ao hostname do upstream falhando conexão em 3333.

## Hipótese aplicada

- Processo Node possivelmente não acessível via IPv4/rede do proxy sem host explícito no `listen`.

## Alterações

- `apps/api/src/server.ts`
  - adicionado `host` (`HOST` env, fallback `0.0.0.0`)
  - alterado `app.listen(port)` para `app.listen(port, host)`
  - log de boot atualizado para exibir host real.

## Comandos executados

- `npm run build:api` (ok)

## Resultado

- Build TypeScript da API sem erro.
- Pendente commit/push e deploy do `api-typebot-crm` para validar fim do 502 externo.

## Pendências

- Commitar/pushar alteração.
- Deploy API e reteste:
  - `https://typebot-api-typebot-crm.achpyp.easypanel.host/health`
  - `https://app.chattypebot.com/health`
