/** Base64 data URL — imagens comprimidas no drawer do lead. */
export const LEAD_ATTACHMENT_IMAGE_MAX_CONTENT_LENGTH = 280_000;

/** Base64 data URL — PDF, Word, Excel e demais documentos. */
export const LEAD_ATTACHMENT_DOCUMENT_MAX_CONTENT_LENGTH = 6_000_000;

/** Tamanho máximo do arquivo bruto antes de codificar em base64. */
export const LEAD_ATTACHMENT_DOCUMENT_MAX_BYTES = 4 * 1024 * 1024;
