# Copiar para `.env.local` na raiz de apps/sales:
#   copy .env.local.example .env.local   (Windows)
#   cp .env.local.example .env.local     (Unix)
#
# O Git ignora `.env.local` — não comite segredos.

VITE_API_BASE_URL=http://localhost:3333
VITE_PAINEL_URL=http://localhost:5173
